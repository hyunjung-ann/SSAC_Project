//
//  ViewController.swift
//  Emoticon_test
//
//  Created by 안현정 on 2021/09/16.
//
import UIKit
/* var emoList = [String]()


class FirstViewController: UIViewController, UITableViewDelegate, UITableViewDataSource {

    
    @IBOutlet weak var TableView: UITableView!

    
    override func viewDidLoad() {
        super.viewDidLoad()
       
        // 앱 구동 초기 기존에 NSUserDefaults에 저장된 데이터를 불러와 emoList 배열에 저장함
        UserDefaults.standard.object(forKey: "emoList") != nil {

            emoList = UserDefaults.standard.object(forKey: "emoList") as! [String]
                }
            }

    
    @IBAction func emoButton(_ sender: Any) {
    }
  
    
        
    // MARK: - table view
    
    func numberOfSections(in tableView: UITableView) -> Int {
        return 1
    }
    
    func tableView(_ tableView: UITableView, numberOfRowsInSection section: Int) -> Int {
        print("emoList count = \(emoList.count)")
        return emoList.count
    }
    
    // emoList 배열에 있는 데이터를 테이블뷰 cell에 넣음
    func tableView(_ tableView: UITableView, cellForRowAt indexPath: IndexPath) -> UITableViewCell {
        let cell = tableView.dequeueReusableCell(withIdentifier: "myCell"), for : indexPath)
        

        let emo = emoList[indexPath.row]
        
        return cell
    }
    

}

 */
